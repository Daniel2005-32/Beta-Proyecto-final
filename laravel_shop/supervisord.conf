[supervisord]
nodaemon=true
user=root
loglevel=info

[program:php-fpm]
command=php-fpm -R
autostart=true
autorestart=true

[program:nginx]
command=nginx -g 'daemon off;'
autostart=true
autorestart=true
